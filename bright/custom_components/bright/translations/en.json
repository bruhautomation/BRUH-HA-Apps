{
  "config": {
    "step": {
      "user": {
        "title": "Set up BRight",
        "description": "This integration talks to the BRight add-on running on this Home Assistant host. It adds the party_mode, start_party, start_show and stop_show services and a show-status sensor."
      },
      "confirm": {
        "title": "BRight discovered",
        "description": "Confirm setup to enable the light-show services and status sensor."
      }
    },
    "abort": {
      "already_configured": "BRight is already configured."
    }
  },
  "entity": {
    "sensor": {
      "show_status": {
        "name": "Show status"
      }
    }
  },
  "services": {
    "party_mode": {
      "name": "Start party mode",
      "description": "Play the configured (or given) music folder on a media player and run each track's compiled light show in sync.",
      "fields": {
        "media_player": {
          "name": "Media player",
          "description": "Which media player plays the music. Defaults to the last one calibrated."
        },
        "folder": {
          "name": "Music folder",
          "description": "Folder under /media to play. Defaults to the add-on's music_folder option."
        },
        "party": {
          "name": "Saved set",
          "description": "Name of a set saved in the BRight panel. Its speaker, folder, songs, light selection and end scene are used; anything given here wins over what the set saved."
        },
        "end_scene": {
          "name": "End scene",
          "description": "A scene to call when the party stops, instead of restoring every light to how it was before."
        },
        "shuffle": {
          "name": "Shuffle",
          "description": "Play the folder in a random order. On by default."
        }
      }
    },
    "start_show": {
      "name": "Start a show",
      "description": "Play one track and its compiled light show.",
      "fields": {
        "track": {
          "name": "Track",
          "description": "Path of the track under /media."
        },
        "media_player": {
          "name": "Media player",
          "description": "Which media player plays the music. Defaults to the last one calibrated."
        }
      }
    },
    "stop_show": {
      "name": "Stop the show",
      "description": "Stop playback and put the room back — restoring every light to its pre-show state, or calling a scene if one is given (or if the running party named one).",
      "fields": {
        "scene": {
          "name": "End scene",
          "description": "Call this scene instead of restoring the lights. Use it when the show was the evening and what comes next is bedtime."
        }
      }
    },
    "start_party": {
      "name": "Start a saved set",
      "description": "Run a set saved in the BRight panel, by name — its speaker, music folder or songs, lights and end scene were set up once and are reused.",
      "fields": {
        "party": {
          "name": "Set",
          "description": "The saved set's name, as it appears in the panel's Party tab."
        },
        "media_player": {
          "name": "Media player",
          "description": "Override the speaker this party normally plays on."
        },
        "end_scene": {
          "name": "End scene",
          "description": "Override the scene called when this party stops."
        }
      }
    }
  }
}
