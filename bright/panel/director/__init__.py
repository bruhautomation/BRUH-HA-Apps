"""The director: turns a track's analysis + the light map into a compiled
show. Two script-writing tiers (algorithmic always, Claude when available),
ONE compiler — a script is data, and the compiler is the only thing that
knows how a scene becomes packets."""
